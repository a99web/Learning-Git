- Adding newline to check if git checkout to some other branch fails or not. Adding some text at the end. Edit by test-1.
- Adding newline to check if git checkout to some other branch fails or not. Edit in test-2.
